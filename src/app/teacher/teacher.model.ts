export class Teacher {
    public id: number;
    public name: string;
    public email: string;
    public courses: any; //Temporal courses array

    constructor(
        id: number,
        name: string,
        email: string
    ) {}
}

